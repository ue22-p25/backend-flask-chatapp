import Link from 'next/link'
import Image from "next/image"

import { Badge } from "@/components/ui/badge"
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"


<div className="container mx-auto py-10">

# A Chat App in Flask - step by step

## Purpose

This repo describes how to build **a chat app with Flask**, from scratch, and
step by step.
Each step outlines a specific feature, and how to implement it.

It's probably best not to try and implement the code yourself, since that would
drain your bandwidth, and in any case the final code is available in the repo below;
instead, you can read through the code and comments, and try to figure out how it
works.

## Layouts

there are 2 layouts available for the material in this activity; depending on
your preference, you can choose either one:

  {/* 1. The Grid Container */}
  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">

    {/* Card 1 */}
    <Link href="/scrollycoding" className="group no-underline text-inherit">
      <Card className="h-full transition-all group-hover:shadow-md
          border-4
          bg-zinc-800
          border-zinc-600
          hover:bg-zinc-600/50
          hover:border-red-600
          transition-colors
      ">
        <CardHeader>
          <CardTitle className="no-underline text-zinc-100 text-xl">Dual-column scrolly-coding (preferred)</CardTitle>
          <CardDescription className="no-underline text-zinc-400">
            You'll get comments on the left hand side and code changes on the right hand side.
          </CardDescription>
        </CardHeader>
      </Card>
    </Link>

    {/* Card 2 */}
    <Link href="/singlecolumn" className="group no-underline text-inherit">
      <Card className="h-full transition-all group-hover:shadow-md
          border-4
          bg-zinc-800
          border-zinc-600
          hover:bg-zinc-600/50
          hover:border-red-600
          transition-colors
      ">
        <CardHeader>
          <CardTitle className="no-underline text-zinc-100 text-xl">Single-column layout (more traditional)</CardTitle>
          <CardDescription className="no-underline text-zinc-400">
            For each step you get a presentation first, and later on the code changes
          </CardDescription>
        </CardHeader>
      </Card>
    </Link>

  </div>

## Implementation

<a
  href="https://github.com/ue22-p25/backend-flask-chatapp-steps"
  target="_blank"
  className="hover:opacity-80 transition-opacity mr-4"
>
  <Badge variant="outline" className="
    shrink-0
    gap-3 px-8 py-0
    border-zinc-700
    bg-zinc-900
    border-2
    hover:border-red-600
    text-zinc-300 text-xl
  ">
    <Image src="/github.svg" alt=""
      width={32} height={32}
      className="invert" />
    The final code (all steps)
  </Badge>
</a>
<a
  href="https://github.com/ue22-p25/backend-flask-chatapp"
  target="_blank"
  className="hover:opacity-80 transition-opacity"
>
  <Badge variant="outline" className="
    shrink-0
    gap-1.5 py-1 px-5
    border-zinc-700
    bg-zinc-900
    border-2
    hover:border-red-600
    text-zinc-300 text-l
">
    <Image src="/github.svg" alt=""
     width={24} height={24}
      className="invert" />
    This book
  </Badge>
</a>

This book is made using <a href="https://nextjs.org/">`Next.js`</a>, the
code snippets are rendered using <a href="https://codehike.org/">`codehike`</a>.

</div>
